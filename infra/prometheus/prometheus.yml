global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: rolldice
    metrics_path: /metrics
    static_configs:
      - targets:
          - host.docker.internal:3000
          - host.docker.internal:3001
        labels:
          service: rolldice
