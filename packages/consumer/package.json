{
  "name": "consumer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "node --import ./src/instrumentation.ts src/index.ts --watch",
    "build": "tsc"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
